databaseChangeLog:
  - changeSet:
      id: create-roles-table
      author: admin
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: role
                  type: enum('USER', 'ADMIN')
                  constraints:
                    nullable: false
                    unique: true